version: "3.9"

services:
  shopping-cart:
    build: .
    container_name: shopping-cart
    ports:
      - "5000:5000"
    environment:
      FLASK_HOST: 0.0.0.0
      PORT: 5000
    restart: unless-stopped